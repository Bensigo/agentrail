{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/runner/Dockerfile"
  },
  "deploy": {
    "startCommand": "agentrail fleet",
    "restartPolicyType": "ALWAYS"
  }
}
