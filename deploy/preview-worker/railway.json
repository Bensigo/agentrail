{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/runner/Dockerfile"
  },
  "deploy": {
    "startCommand": "python3 -m agentrail.runner.preview_worker",
    "restartPolicyType": "ALWAYS"
  }
}
